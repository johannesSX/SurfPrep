# SurfPrep Python dependencies.
#
# FastSurfer and FreeSurfer are NOT pip packages — install FastSurfer via Docker
# (deepmi/fastsurfer:latest, see README) and obtain a free FreeSurfer license.
# FREESURFER_HOME must point at a FreeSurfer install for the mri_* tools used in
# step 2 (or run those inside the FastSurfer container).

numpy>=1.24
scipy>=1.10
nibabel>=5.1
pandas>=2.0
SimpleITK>=2.3
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65
